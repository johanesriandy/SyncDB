org.gradle.jvmargs=-Xmx2048M -Dfile.encoding=UTF-8 -Dkotlin.daemon.jvm.options\="-Xmx1536M"
org.gradle.caching=true
org.gradle.configuration-cache=false

# Kotlin
kotlin.code.style=official
kotlin.daemon.jvmargs=-Xmx1536M

# Android
android.useAndroidX=true
android.nonTransitiveRClass=true
# Installed platform is android-37, newer than AGP 8.5's tested max (35).
# This only suppresses the "untested compileSdk" warning; the build is unaffected.
android.suppressUnsupportedCompileSdk=37

kotlin.native.ignoreDisabledTargets=true
